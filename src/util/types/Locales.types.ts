export type MenuLink = {
  label: string;
  anchor: string;
};

export type SkillsType = {
  headingColor: string;
  title: string;
  desc?: string;
  facts: string[];
};

export type HeroType = {
  title: string;
  subtitle: string;
  btnLabel: string;
};
